# app/core/config.py

class Settings:
    """
    Application settings.
    """
    DEADLINE_CONNECTION_STRING: str = "placeholder_for_deadline_connection"
    # Add other settings here as needed

settings = Settings()